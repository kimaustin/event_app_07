<h1>Edit Invitation</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.invitation_path(@conn, :update, @invitation)) %>

<p><span><%= link "Back to event", to: Routes.event_path(@conn, :show, @invitation.event_id) %></span></p>
<span><%= link "Back to home", to: Routes.page_path(@conn, :index) %></span>
